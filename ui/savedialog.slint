import { StandardButton } from "std-widgets.slint";
export component SaveDialog inherits Dialog {

    Text {
        text: "Your journal is not saved. Would you like to save ?";
    }
    StandardButton { kind: ok; }
    StandardButton { kind: cancel; }

}